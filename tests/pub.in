/* Exemplo de Programa na linguagem L */

int n<-1;
string nome;
const MAXITER=10;

readln(nome);

float percentual;
while n<=MAXITER {
  percentual<-n/MAXITER;
  writeln(float(n),": Ola' ",nome);
  n<-n+1;
}
